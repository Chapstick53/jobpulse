# Dashboard dependencies only — Streamlit Cloud installs this file.
# Unpinned so the installer picks versions compatible with the cloud's Python.
# The data pipeline (GitHub Actions) uses requirements-pipeline.txt.
streamlit
altair
pandas
